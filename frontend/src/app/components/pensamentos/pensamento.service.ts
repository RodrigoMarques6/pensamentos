import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
  // ProvidedIn significa "provedor", isto é, ele pode ser utilizado em todas as partes da aplicação.
})
export class PensamentoService {
  constructor() {}
}
